import { TableEditDirective } from './table-edit.directive';

describe('TableEditDirective', () => {
  it('should create an instance', () => {
    const directive = new TableEditDirective();
    expect(directive).toBeTruthy();
  });
});
