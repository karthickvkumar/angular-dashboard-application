import { CustomEmployeeDirective } from './custom-employee.directive';

describe('CustomEmployeeDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomEmployeeDirective();
    expect(directive).toBeTruthy();
  });
});
